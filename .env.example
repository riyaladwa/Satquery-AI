# SatQuery AI Environment Configuration

# Optional Gemini API Key for Live Multimodal Vision Reasoning
GEMINI_API_KEY=

# Optional custom database URL (defaults to SQLite in data/satquery.db)
# DATABASE_URL=postgresql://user:password@localhost:5432/satquery

# Optional external backend API URL for frontend (leave blank for local dev or unified Vercel deployment)
# VITE_API_URL=
